Here is the diff output from a version control system (e.g., Git) showing the changes made to various files in a project. Below is a summary of the changes:

### Frontend CSS Changes (`src/frontend/css/main.css`)
1. **Removed styles**:
   - Removed global styles for `*`, `:before`, and `:after`.
   - Removed styles for `html`, `html[data-theme="light"]`, `html[data-theme="dark"]`, `code`, and `body`.
   - Removed button styles and navigation bar styles.

2. **Added Tailwind CSS**:
   - Imported Tailwind CSS with `@tailwind base;`, `@tailwind components;`, and `@tailwind utilities;`.
   - Defined CSS variables for colors and button styles in `@layer base`.
   - Added responsive styles for buttons and text elements using Tailwind utility classes in `@layer components`.
   - Added media queries for mobile views.

### New Files and Changes in JavaScript and TypeScript

1. **New Hook (`src/frontend/hooks/useComicActions.ts`)**:
   - Created a new file `useComicActions.ts` that exports a custom hook `useComicActions`.
   - The hook manages comic actions such as checking out, tracking, and deleting comics.
   - It uses `useCallback` for memoizing action handlers.

2. **Updated Server Helpers (`src/frontend/util/ServerHelpers.ts`)**:
   - Updated to use double quotes consistently.
   - Added optional success callback functions to `checkoutComic` and `delComic` functions.
   - Improved error handling and logging.

3. **New gRPC Service Implementations (`src/grpc_server/__init__.py`, `src/grpc_server/auth_service.py`, `src/grpc_server/comics_service.py`)**:
   - Created new files for gRPC server initialization and service implementations.
   - Implemented basic gRPC services for authentication and comics with metadata handling.

4. **New gRPC Server (`src/grpc_server/server.py`)**:
   - Added a new gRPC server setup and execution script.
   - Configured logging and server to run on a specified port.

5. **Updated Text Helper (`src/helpers/text.py`)**:
   - Added a new file `text.py` with functions for normalizing text and title lists.
   - Implemented `normalize_text` and `normalize_title_list` functions to handle text normalization.

6. **Updated Server Logic (`src/server/__init__.py`)**:
   - Updated to use the new `normalize_text` function for title normalization.
   - Integrated the new `normalize_text` function from `helpers.text`.

### Summary
The changes involve a significant frontend overhaul with Tailwind CSS integration, new JavaScript and TypeScript hooks and utilities, and the addition of gRPC service implementations. The project structure has been expanded to include new functionalities for handling comics and authentication services.
